---
import AdminApp from '../../ui/AdminApp.jsx';
import { protectRoute } from '../../lib/auth';

// Asegurar que esta página se renderice en el servidor (no prerenderizada)
export const prerender = false;

// Verificar si hay un parámetro de sincronización
const url = new URL(Astro.request.url);
const needsSync = url.searchParams.get('sync') === 'true';
const justSynced = url.searchParams.get('synced') === 'true';

// Si acaba de sincronizar, esperar un momento para que la cookie se establezca
if (justSynced) {
  await new Promise(resolve => setTimeout(resolve, 100));
}

// Si no necesita sincronización, verificar acceso normalmente
if (!needsSync) {
  const { user, hasAccess, error } = await protectRoute(Astro.cookies, 'admin');

  // Logging para debugging
  console.log('🔐 [Admin] Verificación de acceso:', {
    hasUser: !!user,
    userEmail: user?.email || 'N/A',
    userRole: user?.role || 'N/A',
    hasAccess,
    error: error || 'N/A',
    userId: user?.id || 'N/A',
    justSynced,
    cookiePresent: !!Astro.cookies.get('sb-server-auth-token')?.value
  });

  if (!hasAccess) {
    // Si no hay acceso pero hay sesión en el cliente, mostrar página de sincronización
    // De lo contrario, redirigir a acceso denegado
    // Por ahora, redirigir a acceso denegado
    console.log('❌ [Admin] Acceso denegado, redirigiendo a /access-denied');
    return Astro.redirect('/access-denied');
  }
}

const brand = import.meta.env.PUBLIC_BRAND_NAME || 'LLAVE';
---
{needsSync ? (
  // Página intermedia para sincronizar sesión
  <html lang="es">
    <head>
      <meta charset="utf-8" />
      <meta name="viewport" content="width=device-width, initial-scale=1" />
      <title>Sincronizando sesión...</title>
    </head>
    <body style="display: flex; align-items: center; justify-content: center; min-height: 100vh; font-family: system-ui;">
      <div style="text-align: center;">
        <p>Sincronizando sesión...</p>
      </div>
      <script>
        // Leer sesión directamente de localStorage (Supabase la guarda ahí)
        async function syncAndRedirect() {
          try {
            const sessionStorageKey = 'sb-auth-token';
            const sessionData = localStorage.getItem(sessionStorageKey);
            
            if (!sessionData) {
              console.log('⚠️ [Admin] No hay sesión en localStorage');
              window.location.href = '/access-denied';
              return;
            }

            let session;
            try {
              session = JSON.parse(sessionData);
            } catch (e) {
              console.error('❌ [Admin] Error al parsear sesión:', e);
              window.location.href = '/access-denied';
              return;
            }

            // Supabase guarda la sesión como: { access_token, refresh_token, expires_at, user, ... }
            // O como: { currentSession: { access_token, refresh_token, ... } }
            let access_token, refresh_token;
            
            if (session?.access_token && session?.refresh_token) {
              // Formato directo
              access_token = session.access_token;
              refresh_token = session.refresh_token;
            } else if (session?.currentSession?.access_token && session?.currentSession?.refresh_token) {
              // Formato anidado
              access_token = session.currentSession.access_token;
              refresh_token = session.currentSession.refresh_token;
            } else if (typeof session === 'object' && Object.keys(session).length > 0) {
              // Intentar buscar en cualquier nivel del objeto
              const findTokens = (obj) => {
                if (obj?.access_token && obj?.refresh_token) {
                  return { access_token: obj.access_token, refresh_token: obj.refresh_token };
                }
                for (const key in obj) {
                  if (typeof obj[key] === 'object' && obj[key] !== null) {
                    const found = findTokens(obj[key]);
                    if (found.access_token && found.refresh_token) return found;
                  }
                }
                return {};
              };
              const tokens = findTokens(session);
              access_token = tokens.access_token;
              refresh_token = tokens.refresh_token;
            }

            if (!access_token || !refresh_token) {
              console.log('⚠️ [Admin] No se encontraron tokens de sesión válidos');
              window.location.href = '/access-denied';
              return;
            }

            console.log('🔄 [Admin] Sincronizando sesión con servidor...');
            
            const response = await fetch('/api/auth/session', {
              method: 'POST',
              headers: { 'content-type': 'application/json' },
              credentials: 'same-origin',
              body: JSON.stringify({
                session: {
                  access_token,
                  refresh_token,
                },
              }),
            });
            
              if (response.ok) {
                console.log('✅ [Admin] Sesión sincronizada, esperando un momento antes de redirigir...');
                // Esperar un momento para asegurar que la cookie se estableció
                await new Promise(resolve => setTimeout(resolve, 1000));
                // Redirigir a /admin con parámetro synced=true para indicar que ya se sincronizó
                window.location.href = '/admin?synced=true';
              } else {
              const errorText = await response.text();
              console.error('❌ [Admin] Error al sincronizar sesión:', errorText);
              window.location.href = '/access-denied';
            }
          } catch (error) {
            console.error('❌ [Admin] Error en syncAndRedirect:', error);
            window.location.href = '/access-denied';
          }
        }
        
        syncAndRedirect();
      </script>
    </body>
  </html>
) : (
  // Página normal del admin
  <html lang="es">
    <head>
      <meta charset="utf-8" />
      <meta name="viewport" content="width=device-width, initial-scale=1" />
      <title>Panel de Administración · {brand}</title>
      <link rel="stylesheet" href="/src/styles/global.css" />
    </head>
    <body class="min-h-screen" style={`background:${import.meta.env.PUBLIC_BRAND_COLOR||'#E2905C'}`}>
      <div id="root"><AdminApp client:load /></div>
      <script>
        // Leer sesión directamente de localStorage y sincronizar en segundo plano
        (async function() {
          try {
            const sessionStorageKey = 'sb-auth-token';
            const sessionData = localStorage.getItem(sessionStorageKey);
            
            if (!sessionData) {
              console.log('⚠️ [Admin] No hay sesión en localStorage');
              return;
            }

            let session;
            try {
              session = JSON.parse(sessionData);
            } catch (e) {
              console.warn('⚠️ [Admin] Error al parsear sesión:', e);
              return;
            }

            // Supabase guarda la sesión como: { access_token, refresh_token, expires_at, user, ... }
            // O como: { currentSession: { access_token, refresh_token, ... } }
            let access_token, refresh_token;
            
            if (session?.access_token && session?.refresh_token) {
              access_token = session.access_token;
              refresh_token = session.refresh_token;
            } else if (session?.currentSession?.access_token && session?.currentSession?.refresh_token) {
              access_token = session.currentSession.access_token;
              refresh_token = session.currentSession.refresh_token;
            }

            if (access_token && refresh_token) {
              console.log('🔍 [Admin] Sesión encontrada en cliente, verificando sincronización...');
              // Intentar sincronizar en segundo plano
              const response = await fetch('/api/auth/session', {
                method: 'POST',
                headers: { 'content-type': 'application/json' },
                credentials: 'same-origin',
                body: JSON.stringify({
                  session: {
                    access_token,
                    refresh_token,
                  },
                }),
              });
              
              if (!response.ok) {
                console.warn('⚠️ [Admin] No se pudo sincronizar sesión en segundo plano');
              } else {
                console.log('✅ [Admin] Sesión sincronizada en segundo plano');
              }
            }
          } catch (err) {
            console.warn('⚠️ [Admin] Error al sincronizar sesión en segundo plano:', err);
          }
        })();
      </script>
    </body>
  </html>
)}
